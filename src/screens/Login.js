import { View, Text, Alert, Button } from 'react-native'
import { auth } from '../firebase/config';




const Login = ({ }) => {
    return (
        <View>
            <Text>Login </Text>
            {}

        </View>
    )
}

export default Login